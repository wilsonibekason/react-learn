action creators are funtions thst return other actions


N/B Try to structure your application  so that each feature has its own  substate of data to work on 

when declaring a reducr firstly think ablout th function declaration
reducer takes in the initial / default state which  could be an empty array obj the second paramter is the action

// struture of reducers
1 use of switc statements

rest operator collects data while spread operator retrives or spreads that data